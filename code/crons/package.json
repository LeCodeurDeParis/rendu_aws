{
  "name": "@iim/crons",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir=dist --target=node --format=esm"
  },
  "dependencies": {
    "@iim/domain": "workspace:*",
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
