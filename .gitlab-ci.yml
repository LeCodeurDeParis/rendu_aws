image: node:20

stages:
  - lint
  - deploy

lint_nuxt:
  stage: lint
  script:
    - python3 infrastructure/scripts/lint.nuxt.py

deploy_assets:
  stage: deploy
  needs:
    - lint_nuxt
  script:
    - python3 infrastructure/scripts/deploy.nuxt.py assets
